import { idApp } from "./app";
import { idCommon, idDecision, idNews, idPrice, idSignal } from "./market";
import { idPlan, idPreset } from "./plan";

export const idLocale = {
  app: idApp,
  common: idCommon,
  decision: idDecision,
  price: idPrice,
  signal: idSignal,
  news: idNews,
  plan: idPlan,
  preset: idPreset,
};
