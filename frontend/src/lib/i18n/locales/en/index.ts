import { enApp } from "./app";
import { enCommon, enDecision, enNews, enPrice, enSignal } from "./market";
import { enPlan, enPreset } from "./plan";

export const enLocale = {
  app: enApp,
  common: enCommon,
  decision: enDecision,
  price: enPrice,
  signal: enSignal,
  news: enNews,
  plan: enPlan,
  preset: enPreset,
};
