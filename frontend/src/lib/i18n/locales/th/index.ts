import { thApp } from "./app";
import { thCommon, thDecision, thNews, thPrice, thSignal } from "./market";
import { thPlan, thPreset } from "./plan";

export const thLocale = {
  app: thApp,
  common: thCommon,
  decision: thDecision,
  price: thPrice,
  signal: thSignal,
  news: thNews,
  plan: thPlan,
  preset: thPreset,
};
